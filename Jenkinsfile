pipeline {
    agent any

    stages {
        stage("build") {
            steps {
            echo 'This Builds';
            }
        }
      stage("test"){
        echo 'This tests';
      }
      stage("deploy"){
        echo 'Deploye';
      }
    }
}
